version: '3'
services:
  playground:
    container_name: 'rust'
    hostname: 'rust'
    build: .
    restart: always
    working_dir: '/opt'
    tty: true
    volumes:
      - type: bind
        source: ../opt
        target: /opt
